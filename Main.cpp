// Project 03 - Trees , Trees , and Trees (A Tale of a Forest)
// by:  Manuel Martinez , John Santoro , and Jim Bui

#include "Menu_Program.h"

// void empty(MaxHeapTree<std::string> tree_in)
// {
// 	std::cout << "empty()?: " ;
// 	if (tree_in.empty()) std::cout << "yes. \n" ;
// 	else std::cout << "no. \n" ;
// }

// void getMax(MaxHeapTree<std::string> tree_in)
// {
// 	std::cout << "getMax(): " << tree_in.getMax() << " \n" ;
// }

// void getHeight(MaxHeapTree<std::string> tree_in)
// {
// 	std::cout << "getHeight(): " << tree_in.getHeight() << " \n" ;
// }

// void getSize(MaxHeapTree<std::string> tree_in)
// {
// 	std::cout << "getSize(): " << tree_in.getSize() << " \n" ;
// }

// void leaves(MaxHeapTree<std::string> tree_in)
// {
// 	std::cout << "leaves(): " << tree_in.leaves() << " \n" ;
// }

// void test(MaxHeapTree<std::string> tree_in)
// {
// 	tree_in.display() ;
// 	empty(tree_in) ; // this ok.
// 	getMax(tree_in) ; // this broke.
// 	getHeight(tree_in) ; 
// 	getSize(tree_in) ;
// 	leaves(tree_in) ;
// 	std::cout << " \n" ;
// }

int main()
{
	Menu_Program() ;
	// std::cout << "\n" << "hello world!" << "\n\n" ;

	// MaxHeapTree<std::string> the_heap(1) ; // elements + 1

	// the_heap.buildTreehouse("MaxHeapTreeText.txt") ;
	// test(the_heap) ;

	// the_heap.insert(5 , "cersei") ;
	// the_heap.insert(3 , "jamie") ;
	// the_heap.insert(19 , "ned") ;
	// the_heap.insert(20 , "rob") ;
	// the_heap.insert(4 , "flint") ;

	// the_heap.insert(6 , "sheppard") ;
	// the_heap.insert(10 , "kitty") ;
	// the_heap.insert(7 , "grils") ;
	// the_heap.insert(30 , "front") ;
	// the_heap.insert(190 , "back") ;

	// the_heap.insert(200 , "left") ;
	// the_heap.insert(40 , "right") ;
	// the_heap.insert(60 , "up") ;
	// the_heap.insert(100 , "down") ;
	// the_heap.insert(70 , "square") ;

	// test(the_heap) ; // ok

	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;

	// test(the_heap) ; // no go.

	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;

	// test(the_heap) ;

	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;
	// the_heap.delMax() ;

	// test(the_heap) ;

	return 0 ;
}